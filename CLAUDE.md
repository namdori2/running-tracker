# 앵그리러너스 (Angry Runners) — 제품 기획

러닝 기록 앱에서 발전시킨 게임화(gamification) + 소셜 러닝 앱. 아래는 사용자가 정의한
목표 기능 스펙이며, 향후 작업 시 이 문서를 기준으로 삼는다.

## 현재 구현 상태 (기초 러닝 트래커)

- `mobile/` — Expo(React Native) 앱. Leaflet+OpenStreetMap 지도(WebView)로 현재 위치 표시,
  `expo-location`으로 실시간 위치 추적, 러닝 시작/정지, 거리·시간·페이스 계산.
- `server/` — Express + better-sqlite3. `/api/runs` CRUD만 존재 (유저 개념 없음, 단일 기기 기준).
- 인증/계정 시스템 없음. 지역 개념 없음. 소셜 기능 없음.

아래 기능들은 **모두 미구현**이며, 이 문서에 정의된 대로 순차 개발 예정.

## 목표 기능 스펙

### 1. 레벨 시스템
- 일간 러닝 퀘스트 수행 또는 그냥 뛰기만 해도 경험치(XP) 획득
- 경험치 누적으로 레벨업

### 2. 지역별 랭킹 시스템
- 유저 이름 옆에 소속 지역(구/동 단위) 랭킹 표시
  - 예: "남승윤 (송파구 2위)"
- 인스타그램 등 SNS 공유 기능 (랭킹/기록 카드 이미지 공유)

### 3. 러닝 맵(코스) 선택 기능
- 유저들이 직접 만든 러닝 코스를 "맵"으로 등록하고, 다른 유저가 그 코스를 선택해서 플레이 가능
- 인기 맵 추천 기능 (플레이 수/평점 기반)
- 맵별 개별 랭킹
  - 예: "불타는 석촌호수 (1위 000, 2위 000)"

### 4. 근처 러너 알림
- 상위 랭커가 주변에 있으면 다른 러너들에게 푸시 알림 발송
  - 예: "송파구 3위 000님이 주변에 있습니다 함께 달려보아요!"
- 백그라운드 위치 기반 근접 감지 필요 (배터리/권한/프라이버시 이슈 고려 필요)

### 5. 러너 프로필
- 프로필 페이지 활성화 (기록, 레벨, 지역 랭킹, 뱃지 등 표시)
- "응원하기" 버튼으로 다른 러너를 응원 가능 (좋아요/격려 상호작용)

## 구현상 선결 과제 (미논의 항목)

실제 개발 착수 전 결정이 필요한 사항들:

- **유저 인증**: 현재 계정 시스템이 전혀 없음. 랭킹/프로필/응원 기능 모두 로그인 필요.
  (자체 이메일 인증 vs 소셜 로그인(카카오/구글/Apple) 등 방식 결정 필요)
- **지역(구/동) 판정 방식**: GPS 좌표 → 행정구역 역지오코딩 필요 (예: Kakao 로컬 API, 또는
  OSM Nominatim)
- **커뮤니티 맵 데이터 모델**: 코스 등록/좌표 경로 저장/플레이 기록 매칭 방식 설계 필요
- **근처 러너 감지**: 실시간(또는 준실시간) 위치 공유 인프라, 백그라운드 위치 권한(iOS는
  특히 까다로움), 알림 발송 트리거 정책 (배터리 소모, 프라이버시 동의 UX)
- **푸시 알림 인프라**: Expo Push Notification 서비스 사용 예정
- **SNS 공유**: 인스타그램 스토리/피드 공유는 이미지 생성 후 OS 공유 시트(Share Sheet) 또는
  인스타그램 URL 스킴 사용

## 진행 방식

기능 범위가 크므로 한 번에 다 구현하지 않고 단계별로 진행한다. 각 단계 완료 후 다음 단계로
넘어가기 전 사용자와 우선순위/방식을 재확인한다.

## 결정된 사항 (2026-09-20)

- **1단계 개발 순서**: 계정/로그인 → 레벨·XP 시스템 → 지역 랭킹 → 커뮤니티 맵 → 근처 러너
  알림/응원하기 순으로 진행.
- **로그인 방식**: 소셜 로그인(카카오/구글). 단, **카카오를 먼저 구현**한다.
  - 이유: OAuth 리다이렉트 URI는 서버(백엔드) 주소로 등록해야 하는데, 개발 중에는 서버가
    PC의 LAN IP(`http://192.168.x.x:4000`)로 떠 있다. 카카오는 개발 단계에서 http/IP 기반
    리다이렉트 URI 등록을 허용하지만, 구글 OAuth는 보통 `localhost` 또는 HTTPS 도메인만
    허용하고 임의의 사설 IP는 거부하는 경우가 많다. 따라서 구글 로그인을 추가하려면 ngrok
    등으로 HTTPS 터널을 열거나 실제 도메인을 배포해야 한다 (추후 과제).
- **로그인 플로우**: Expo Go 환경(네이티브 커스텀 모듈/딥링크 불가)에서도 동작하도록,
  앱은 로그인 세션 UUID를 만들어 브라우저(WebBrowser)로 `서버주소/auth/kakao/start?session=UUID`
  를 열고, 서버가 카카오 OAuth를 처리한 뒤 로그인 성공 시 해당 세션에 JWT를 저장한다.
  앱은 `서버주소/auth/session/:sessionId` 를 폴링해서 로그인 완료 여부와 토큰을 받아온다.
  (커스텀 URL 스킴 딥링크 대신 폴링 방식을 쓰는 이유: EAS 개발 빌드 없이 Expo Go만으로
  전체 플로우가 동작해야 하기 때문. 이전 세션에서 EAS 클라우드 빌드 시도 중 Apple 계정
  잠김 등 문제를 겪어 네이티브 빌드 의존을 최소화하기로 함.)
- **XP/레벨 공식**: 러닝 1회 완료 시 `distance_km * 10 + duration_min * 1` XP 획득.
  하루 첫 러닝에는 "일간 퀘스트" 보너스 +50 XP 추가. 레벨업 임계값은 누적 XP 기준
  `100 * level^2` (레벨 1 = 100 XP, 레벨 2 = 400 XP 누적, ...).
